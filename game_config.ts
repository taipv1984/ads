export const GAME_CONFIG = {
  // Hệ tọa độ ảo (Virtual Coordinate System) dùng để vẽ chuẩn trước khi scale lên màn hình thực tế
  virtualWidth: 1000,
  virtualHeight: 1000,

  // Có thể bổ sung thêm config khác ở đây như font size base, base shapes...
};
