export enum ViewMode {
    EDIT = 'edit',
    REVIEW = 'review',
}

export enum QuestionType {
    FILL = 'fill',
    MATCH = 'match',
    SELECT = 'select',
    SORT = 'sort',
    QUIZ = 'quiz',
    STEP = 'step',
}

export enum ElementGroup {
    TOP = 'top',
    BOTTOM = 'bottom',
    LEFT = 'left',
    RIGHT = 'right',
    MASTER = 'master',
}

export enum ValueType {
    SINGLE = 'single',
    MULTI = 'multi',
}